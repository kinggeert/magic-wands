# magic-wands

This is a small magic wands plugin I made in my freetime.

To obtain the wand execute the command "/getwand".
to use a spell use left click, to change the selected spell use left click.
